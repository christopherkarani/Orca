# Edge Container

The Edge container recipe is for CI, fake adapter scenarios, and local simulation checks.
It does not mount real hardware devices by default, does not run privileged by default, does not use host networking by default, and does not include credentials or telemetry.

Container use does not imply production flight deployment.
