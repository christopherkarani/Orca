# Zig security behavior checklist (mode D DoD)
# Plan: planning/100-percent-test-coverage-agent-plan.md §3.2 / §6
# owner: christopherkarani
# status values: todo | ci-green
# assert_kind: decision | exit | wire | deny_reason | fail_closed
#
# Module-done (mode D): all rows for that module are ci-green with test_filter
# names that ./scripts/zig build test actually runs. Pub-fn counting is not DoD.
# Do not mark ci-green without a real filter. Prefer residual gaps as todo.

# ---------------------------------------------------------------------------
# hook — daemon-down / fail-closed shell (shared route; Host wire where present)
# ---------------------------------------------------------------------------

[[row]]
module = "hook"
slice = "hook-residual-fail-closed"
behavior = "shell command blocks when daemon unavailable"
host = "any"
event = "PreToolUse"
expected = "deny/block; no allow"
test_filter = "hook daemon unavailable blocks shell command"
assert_kind = "fail_closed"
status = "ci-green"

[[row]]
module = "hook"
slice = "hook-residual-fail-closed"
behavior = "shell command blocks via fail-closed route when daemon unavailable"
host = "any"
event = "PreToolUse"
expected = "fail_closed route; deny"
test_filter = "hook daemon unavailable blocks shell command via fail-closed route"
assert_kind = "fail_closed"
status = "ci-green"

[[row]]
module = "hook"
slice = "hook-residual-fail-closed"
behavior = "shell command blocks on daemon timeout"
host = "any"
event = "PreToolUse"
expected = "deny/block"
test_filter = "hook daemon timeout blocks shell command via fail-closed route"
assert_kind = "fail_closed"
status = "ci-green"

[[row]]
module = "hook"
slice = "hook-residual-fail-closed"
behavior = "shell command blocks on daemon protocol mismatch"
host = "any"
event = "PreToolUse"
expected = "deny/block"
test_filter = "hook daemon protocol mismatch blocks shell command via fail-closed route"
assert_kind = "fail_closed"
status = "ci-green"

[[row]]
module = "hook"
slice = "hook-residual-fail-closed"
behavior = "shell command blocks on malformed daemon response"
host = "any"
event = "PreToolUse"
expected = "deny/block"
test_filter = "hook daemon malformed response blocks shell command"
assert_kind = "fail_closed"
status = "ci-green"

[[row]]
module = "hook"
slice = "hook-residual-fail-closed"
behavior = "daemon Error status does not allow shell command"
host = "any"
event = "PreToolUse"
expected = "not allow"
test_filter = "hook daemon Error does not produce allow for shell command"
assert_kind = "fail_closed"
status = "ci-green"

[[row]]
module = "hook"
slice = "hook-residual-fail-closed"
behavior = "observe mode still fails closed when daemon unavailable"
host = "any"
event = "PreToolUse"
expected = "deny/block under observe"
test_filter = "hook observe mode fails closed when daemon unavailable"
assert_kind = "fail_closed"
status = "ci-green"

[[row]]
module = "hook"
slice = "hook-residual-fail-closed"
behavior = "pre-eval fail-closed gate covers PreToolUse PermissionRequest and Codex"
host = "codex"
event = "PreToolUse"
expected = "fail closed before eval"
test_filter = "hook pre-eval fail-closed gate covers PreToolUse PermissionRequest and Codex"
assert_kind = "fail_closed"
status = "ci-green"

[[row]]
module = "hook"
slice = "hook-residual-fail-closed"
behavior = "pre-eval fail-closed Codex emits sentinel and exit 2"
host = "codex"
event = "PreToolUse"
expected = "exit 2 + sentinel; no allow JSON on stdout"
test_filter = "hook pre-eval fail-closed Codex emits sentinel and exit 2"
assert_kind = "exit"
status = "ci-green"

[[row]]
module = "hook"
slice = "hook-residual-fail-closed"
behavior = "pre-eval fail-closed Claude emits block JSON on stdout"
host = "claude"
event = "PreToolUse"
expected = "block decision on wire"
test_filter = "hook pre-eval fail-closed Claude emits block JSON on stdout"
assert_kind = "wire"
status = "ci-green"

[[row]]
module = "hook"
slice = "hook-residual-fail-closed"
behavior = "malformed shell payload fail-closes before daemon call"
host = "any"
event = "PreToolUse"
expected = "fail closed; no daemon Evaluate"
test_filter = "hook evaluatePreToolUse fail-closes malformed shell payload before daemon call"
assert_kind = "fail_closed"
status = "ci-green"

[[row]]
module = "hook"
slice = "hook-residual-fail-closed"
behavior = "missing/empty shell command classified fail-closed"
host = "any"
event = "PreToolUse"
expected = "fail_closed route"
test_filter = "hook classifies shell-like missing command as malformed and fail-closed route"
assert_kind = "fail_closed"
status = "ci-green"

[[row]]
module = "hook"
slice = "hook-residual-fail-closed"
behavior = "codex shell deny uses exit code 2"
host = "codex"
event = "PreToolUse"
expected = "exit 2"
test_filter = "hook codex shell deny uses exit code 2"
assert_kind = "exit"
status = "ci-green"

# Residual: per-host daemon-down on production CLI entry (not only shared runShellRoute).
# phase2f covers multi-host when daemon cannot start — map journey; keep explicit todos.

[[row]]
module = "hook"
slice = "hook-residual-fail-closed"
behavior = "opencode shell PreToolUse denies when daemon unavailable (host wire)"
host = "opencode"
event = "tool.execute.before"
expected = "host-visible deny/block"
test_filter = "TODO: opencode daemon-down host wire (extend unit or phase2f filter)"
assert_kind = "fail_closed"
status = "todo"

[[row]]
module = "hook"
slice = "hook-residual-fail-closed"
behavior = "openclaw shell tool.before denies when daemon unavailable (host wire)"
host = "openclaw"
event = "tool.before"
expected = "host-visible deny/block"
test_filter = "TODO: openclaw daemon-down host wire"
assert_kind = "fail_closed"
status = "todo"

[[row]]
module = "hook"
slice = "hook-residual-fail-closed"
behavior = "hermes pre_tool_call shell denies when daemon unavailable (host wire)"
host = "hermes"
event = "pre_tool_call"
expected = "host-visible deny/block"
test_filter = "TODO: hermes daemon-down host wire"
assert_kind = "fail_closed"
status = "todo"

[[row]]
module = "hook"
slice = "hook-residual-fail-closed"
behavior = "claude shell PreToolUse denies when daemon unavailable (host wire beyond pre-eval)"
host = "claude"
event = "PreToolUse"
expected = "block JSON; no allow"
test_filter = "TODO: claude shell daemon-down host wire (if not covered by phase2f alone)"
assert_kind = "wire"
status = "todo"

# ---------------------------------------------------------------------------
# intercept / agent_hook / shim — fail_closed not overridden
# ---------------------------------------------------------------------------

[[row]]
module = "intercept"
slice = "intercept-fail-closed-approval"
behavior = "agent_hook evaluatePayload fails closed on daemon evaluate failures"
host = "any"
event = "shell"
expected = "deny; not allow"
test_filter = "evaluatePayload fails closed on daemon evaluate failures"
assert_kind = "fail_closed"
status = "ci-green"

[[row]]
module = "intercept"
slice = "intercept-fail-closed-approval"
behavior = "shim fails closed on daemon evaluate failures"
host = "any"
event = "shell"
expected = "deny; parent approval must not override fail_closed"
test_filter = "shim fails closed on daemon evaluate failures"
assert_kind = "fail_closed"
status = "ci-green"

# ---------------------------------------------------------------------------
# mcp — proxy fail-closed
# ---------------------------------------------------------------------------

[[row]]
module = "mcp"
slice = "mcp-proxy-fail-closed"
behavior = "invalid json-rpc fails safely with protocol error"
host = "any"
event = "jsonrpc"
expected = "protocol error; no unsafe forward"
test_filter = "invalid json-rpc fails safely with protocol error response"
assert_kind = "fail_closed"
status = "ci-green"

[[row]]
module = "mcp"
slice = "mcp-proxy-fail-closed"
behavior = "malformed MCP transport fails proxy run"
host = "any"
event = "transport"
expected = "protocol error; fail closed"
test_filter = "malformed MCP transport input reports protocol error and fails proxy run"
assert_kind = "fail_closed"
status = "ci-green"

[[row]]
module = "mcp"
slice = "mcp-proxy-fail-closed"
behavior = "proxy fails closed for high-volume tools/list"
host = "any"
event = "tools/list"
expected = "fail closed / bounded"
test_filter = "proxy fails closed for high-volume tools/list responses"
assert_kind = "fail_closed"
status = "ci-green"

[[row]]
module = "mcp"
slice = "mcp-proxy-fail-closed"
behavior = "prompts get deny returns json-rpc error without forwarding"
host = "any"
event = "prompts/get"
expected = "error response; no forward"
test_filter = "prompts get deny returns json-rpc error without forwarding"
assert_kind = "decision"
status = "ci-green"

# Residual MCP rows for later slices
[[row]]
module = "mcp"
slice = "mcp-proxy-fail-closed"
behavior = "upstream/child process death fail-closed (if not covered)"
host = "any"
event = "transport"
expected = "deny/error; no silent allow"
test_filter = "TODO: mcp upstream death fail-closed residual"
assert_kind = "fail_closed"
status = "todo"

# ---------------------------------------------------------------------------
# sandbox — fail-closed apply / scrub / workspace
# ---------------------------------------------------------------------------

[[row]]
module = "sandbox"
slice = "sandbox-fail-closed"
behavior = "mode on without usable Landlock fails closed"
host = "any"
event = "apply"
expected = "RequireFailed"
test_filter = "mode on without usable Landlock fails closed with RequireFailed"
assert_kind = "fail_closed"
status = "ci-green"

[[row]]
module = "sandbox"
slice = "sandbox-fail-closed"
behavior = "mode on + invalid workspace fails closed"
host = "any"
event = "apply"
expected = "fail closed"
test_filter = "mode on + invalid workspace fails closed"
assert_kind = "fail_closed"
status = "ci-green"

[[row]]
module = "sandbox"
slice = "sandbox-fail-closed"
behavior = "mode on and auto fail closed when env scrub incomplete"
host = "any"
event = "apply"
expected = "fail closed"
test_filter = "mode on and auto fail closed when env scrub is incomplete"
assert_kind = "fail_closed"
status = "ci-green"

[[row]]
module = "sandbox"
slice = "sandbox-fail-closed"
behavior = "empty and relative workspace fail closed"
host = "any"
event = "profile"
expected = "fail closed"
test_filter = "P1-U-06 empty and relative workspace fail closed"
assert_kind = "fail_closed"
status = "ci-green"

[[row]]
module = "sandbox"
slice = "sandbox-fail-closed"
behavior = "scrubEnvMapInPlace fails closed on OOM mid-scrub"
host = "any"
event = "env_scrub"
expected = "error; caller fail closed"
test_filter = "scrubEnvMapInPlace fails closed on OOM mid-scrub"
assert_kind = "fail_closed"
status = "ci-green"

# ---------------------------------------------------------------------------
# policy / audit — seed placeholders for later slices (expand when slice opens)
# ---------------------------------------------------------------------------

[[row]]
module = "policy"
slice = "policy-load-validate"
behavior = "invalid policies fail closed with clear parser errors"
host = "any"
event = "load"
expected = "error; no silent accept"
test_filter = "invalid policies fail closed with clear parser errors"
assert_kind = "fail_closed"
status = "ci-green"

[[row]]
module = "audit"
slice = "audit-redaction"
behavior = "presentation redaction residual (expand when slice opens)"
host = "any"
event = "audit"
expected = "no raw secret-shaped output"
test_filter = "TODO: audit redaction residual inventory"
assert_kind = "deny_reason"
status = "todo"
