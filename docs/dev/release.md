# Release Notes

Phase 02 is not releasable as a security product.

Before v1.0 release artifacts are produced, the project must select a license, keep the Zig version pinned, document build reproducibility, and generate checksums.
