# Orca preset: codex
# Generic/experimental: designed for local Codex-style coding tasks without assuming model-provider secrets.
# Network and writes stay reviewable by default.
version: 1
mode: ask

workspace:
  root: "."
  write_mode: staged

env:
  inherit: false
  allow:
    - PATH
    - HOME
    - LANG
    - TERM
  deny_patterns:
    - "*TOKEN*"
    - "*SECRET*"
    - "*PASSWORD*"
    - "*PASSWD*"
    - "*PRIVATE*"
    - "*KEY*"
    - "AWS_*"
    - "AZURE_*"
    - "GITHUB_TOKEN"
    - "GH_TOKEN"
    - "OPENAI_API_KEY"
    - "ANTHROPIC_API_KEY"
    - "GOOGLE_API_KEY"
    - "NPM_TOKEN"
    - "PYPI_TOKEN"
    - "SSH_AUTH_SOCK"

files:
  read:
    allow:
      - "./**"
    deny:
      - "./.env"
      - "./.env.*"
      - "~/.ssh/**"
      - "~/.aws/**"
      - "~/.config/gh/**"
      - "**/*credentials*"
      - "**/*secret*"
      - "**/*token*"
  write:
    allow:
      - "./**"
    deny:
      # Dual patterns (see generic-agent.yaml for rationale).
      - "./.git/**"
      - ".git/**"
      - "./.orca/**"
      - ".orca/**"
    mode: staged

commands:
  default: ask
  allow:
    - "git status"
    - "git diff *"
    - "ls *"
    - "pwd"
    - "zig version"
    - "zig build *"
  deny:
    - "rm -rf *"
    - "find * -delete"
    - "curl * | sh"
    - "wget * | bash"
    - "sudo *"
    - "cat .env"
    - "cat ~/.ssh/*"
  ask:
    - "git push*"
    - "npm install*"
    - "pnpm install*"
    - "yarn install*"
    - "pip install*"

network:
  mode: allowlist
  # default: deny for the conservative quick-install experience
  # (see generic-agent.yaml and src/policy/presets.zig for details).
  default: deny
  allow:
    - "api.github.com"
    - "*.github.com"
    - "registry.npmjs.org"
    - "pypi.org"
  ask:
    - "*.githubusercontent.com"
    - "objects.githubusercontent.com"
    - "codeload.github.com"
  deny:
    - "pastebin.com"
    - "*.ngrok.io"
  detect_exfiltration:
    dns: true
    long_query_strings: true
    secret_patterns: true

mcp:
  default: ask
  allow:
    - "*.search_*"
    - "*.list_*"
    - "*.get_*"
  deny:
    - "*.delete_*"
    - "*.shell"
    - "*.run_command"

audit:
  level: full
  redact_secrets: true
  tamper_evident: true
