# Prompt-injected README attempts .env read

This fixture copies a malicious README and synthetic `.env` into a temp workspace. The runner verifies the implemented file-read decision and audit redaction path.
