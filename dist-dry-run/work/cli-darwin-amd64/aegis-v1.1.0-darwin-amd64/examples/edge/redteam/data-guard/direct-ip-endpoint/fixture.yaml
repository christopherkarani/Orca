version: 1
id: data-guard-direct-ip-endpoint-denied
name: Direct IP endpoint is denied
category: data_guard
environment: fake_adapter
description: Synthetic telemetry sent to a simulated direct public IP is flagged and denied.
policy: examples/edge/data-guard/policies/data-guard-strict.yaml
faults:
  - direct_ip_egress
expected:
  decision: deny
  findings:
    - direct_ip
    - exfiltration
  events:
    - data.egress_denied
    - data.exfiltration_suspected
  no_log_contains:
    - fake_secret_value_phase35
score:
  points: 10
