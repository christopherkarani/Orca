version: 1
id: battery-takeoff-below-threshold-denied
name: Takeoff below battery threshold denied
category: battery
environment: fake_adapter
description: Synthetic takeoff request is evaluated with low battery state.
policy: examples/edge/redteam/policies/redteam-envelope.yaml
faults:
  - low_battery
expected:
  decision: deny
  findings:
    - battery
  events:
    - safety.battery_constraint
    - vehicle.command_denied
  no_log_contains:
    - fake-secret-value
score:
  points: 10
