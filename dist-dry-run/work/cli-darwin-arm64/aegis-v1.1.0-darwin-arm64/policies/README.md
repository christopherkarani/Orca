# Policies

Sample and preset policy files live here.

Useful commands:

```bash
aegis policy check policies/presets/generic-agent.yaml
aegis init --preset generic-agent
```

Preset policies under `policies/presets/` are conservative editable starting points. They do not contain real secrets and do not require external services to validate.
