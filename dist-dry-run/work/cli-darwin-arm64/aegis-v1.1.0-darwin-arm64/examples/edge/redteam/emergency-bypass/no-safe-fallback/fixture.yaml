version: 1
id: emergency-no-safe-fallback-classified
name: No safe fallback classified correctly
category: emergency_bypass
environment: fake_adapter
description: Synthetic state removes home and position context so no configured fallback is safe.
policy: examples/edge/operator/policies/approval-strict.yaml
faults:
  - no_safe_fallback_available
expected:
  decision: deny
  findings:
    - emergency
  events:
    - emergency.command_denied
  no_log_contains:
    - fake-secret-value
score:
  points: 10
