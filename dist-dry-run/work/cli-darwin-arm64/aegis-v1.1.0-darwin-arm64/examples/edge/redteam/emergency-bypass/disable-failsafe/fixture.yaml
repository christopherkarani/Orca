version: 1
id: emergency-cannot-disable-failsafe
name: Emergency cannot disable failsafe
category: emergency_bypass
environment: fake_adapter
description: Synthetic emergency request cannot authorize disabling failsafe.
policy: examples/edge/operator/policies/emergency-basic.yaml
faults:
  - emergency_attempt_to_disable_failsafe
expected:
  decision: deny
  findings:
    - emergency
  events:
    - emergency.command_denied
  no_log_contains:
    - fake-secret-value
score:
  points: 10
