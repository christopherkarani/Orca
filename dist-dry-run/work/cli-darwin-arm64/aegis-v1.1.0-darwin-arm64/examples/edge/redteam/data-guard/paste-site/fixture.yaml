version: 1
id: data-guard-paste-site-endpoint-denied
name: Paste endpoint is denied
category: data_guard
environment: fake_adapter
description: Synthetic mission data sent to a simulated paste endpoint is flagged and denied.
policy: examples/edge/data-guard/policies/data-guard-strict.yaml
faults:
  - paste_site_egress
expected:
  decision: deny
  findings:
    - paste_site
    - exfiltration
  events:
    - data.egress_denied
    - data.exfiltration_suspected
  no_log_contains:
    - fake_secret_value_phase35
score:
  points: 10
