version: 1
id: data-guard-repeated-unknown-endpoint-denied
name: Repeated unknown endpoint attempts are flagged
category: data_guard
environment: fake_adapter
description: Synthetic repeated unknown endpoint attempts are detected without making network calls.
policy: examples/edge/data-guard/policies/data-guard-strict.yaml
faults:
  - repeated_unknown_endpoint_egress
expected:
  decision: deny
  findings:
    - exfiltration
    - unknown
  events:
    - data.egress_denied
    - data.exfiltration_suspected
  no_log_contains:
    - fake_secret_value_phase35
score:
  points: 10
