version: 1
id: approval-mismatched-policy-denied
name: Mismatched policy approval denied
category: approval_bypass
environment: fake_adapter
description: Synthetic approval with a mismatched policy hash is denied.
policy: examples/edge/operator/policies/approval-strict.yaml
faults:
  - mismatched_policy_hash
expected:
  decision: deny
  events:
    - operator.approval_invalid
  no_log_contains:
    - fake-secret-value
score:
  points: 10
