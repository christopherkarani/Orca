version: 1
id: data-guard-safety-report-customer-allowed
name: Safety report to explicit customer endpoint is allowed
category: data_guard
environment: fake_adapter
description: Redacted safety report telemetry is allowed only to an explicitly configured simulated customer endpoint.
policy: examples/edge/data-guard/policies/data-guard-strict.yaml
faults:
  - safety_report_customer_allow
expected:
  decision: allow
  findings:
    - safety_finding
    - customer_endpoint
  events:
    - data.egress_allowed
    - telemetry.channel_allowed
  no_log_contains:
    - fake_secret_value_phase35
score:
  points: 10
