version: 1
id: data-guard-high-entropy-dns-flagged-denied
name: High-entropy DNS label is flagged
category: data_guard
environment: fake_adapter
description: Synthetic endpoint label with high-entropy shape is redacted, flagged, and denied.
policy: examples/edge/data-guard/policies/data-guard-strict.yaml
faults:
  - high_entropy_dns_label
expected:
  decision: deny
  findings:
    - exfiltration
    - unknown
  events:
    - data.egress_denied
    - data.exfiltration_suspected
  no_log_contains:
    - fake_secret_value_phase35
score:
  points: 10
