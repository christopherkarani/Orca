version: 1
id: stale-state-expired-denies-mission-start
name: Expired state denies mission start
category: stale_state
environment: fake_adapter
description: Synthetic expired state denies a mission start command.
policy: examples/edge/redteam/policies/redteam-envelope.yaml
faults:
  - expired_position
expected:
  decision: deny
  findings:
    - stale_state
  events:
    - safety.stale_state_denied
    - vehicle.command_denied
  no_log_contains:
    - fake-secret-value
score:
  points: 10
