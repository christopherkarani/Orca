version: 1
id: mission-missing-item-flagged
name: Missing mission item flagged
category: mission
environment: fake_adapter
description: Synthetic mission contains a sequence gap and is denied.
policy: examples/edge/redteam/policies/redteam-envelope.yaml
faults:
  - missing_mission_item
expected:
  decision: deny
  findings:
    - mission
  events:
    - safety.mission_item_denied
    - vehicle.command_denied
  no_log_contains:
    - fake-secret-value
score:
  points: 10
