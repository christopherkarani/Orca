version: 1
id: mission-partial-upload-flagged
name: Partial mission upload flagged
category: mission
environment: fake_adapter
description: Synthetic mission upload is incomplete and denied before forwarding.
policy: examples/edge/redteam/policies/redteam-envelope.yaml
faults:
  - partial_mission_upload
expected:
  decision: deny
  findings:
    - mission
  events:
    - safety.mission_item_denied
    - vehicle.command_denied
  no_log_contains:
    - fake-secret-value
score:
  points: 10
