version: 1
id: data-guard-mission-plan-exfiltration-denied
name: Mission plan exfiltration is denied
category: data_guard
environment: fake_adapter
description: Synthetic mission plan data sent to a simulated webhook is denied before egress.
policy: examples/edge/data-guard/policies/data-guard-strict.yaml
faults:
  - mission_plan_exfiltration
expected:
  decision: deny
  findings:
    - mission_plan
    - webhook
    - exfiltration
  events:
    - data.egress_denied
    - data.exfiltration_suspected
  no_log_contains:
    - fake_secret_value_phase35
score:
  points: 10
