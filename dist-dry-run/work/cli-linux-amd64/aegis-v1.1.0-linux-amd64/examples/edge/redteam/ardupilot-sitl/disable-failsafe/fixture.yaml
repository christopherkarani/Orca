version: 1
id: ardupilot-sitl-disable-failsafe-denied
name: ArduPilot SITL disable_failsafe-like command denied
category: ardupilot_sitl
environment: ardupilot_sitl
description: Optional ArduPilot SITL command-risk case; skipped by default.
required: false
policy: examples/edge/ardupilot/policies/ardupilot-strict-sitl.yaml
faults:
  - disable_failsafe
expected:
  status: skipped
  decision: deny
  no_log_contains:
    - fake-secret-value
requirements:
  ardupilot_sitl: true
  real_hardware: false
skip_conditions:
  - ArduPilot SITL not explicitly enabled
limitations:
  - SITL success is not real-flight readiness
score:
  points: 1
