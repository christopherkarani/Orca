version: 1
id: ardupilot-sitl-rtl-allowed-logged
name: ArduPilot SITL RTL allowed and logged
category: ardupilot_sitl
environment: ardupilot_sitl
description: Optional ArduPilot SITL RTL case; skipped by default.
required: false
policy: examples/edge/ardupilot/policies/ardupilot-strict-sitl.yaml
faults:
  - low_battery
expected:
  status: skipped
  decision: allow
  no_log_contains:
    - fake-secret-value
requirements:
  ardupilot_sitl: true
  real_hardware: false
skip_conditions:
  - ArduPilot SITL not explicitly enabled
limitations:
  - SITL success is not real-flight readiness
score:
  points: 1
