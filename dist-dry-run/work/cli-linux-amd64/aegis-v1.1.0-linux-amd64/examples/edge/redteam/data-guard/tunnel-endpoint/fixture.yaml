version: 1
id: data-guard-tunnel-endpoint-denied
name: Tunnel endpoint is denied
category: data_guard
environment: fake_adapter
description: Synthetic telemetry sent to a simulated tunnel endpoint is flagged and denied.
policy: examples/edge/data-guard/policies/data-guard-strict.yaml
faults:
  - tunnel_egress
expected:
  decision: deny
  findings:
    - tunnel_service
    - exfiltration
  events:
    - data.egress_denied
    - data.exfiltration_suspected
  no_log_contains:
    - fake_secret_value_phase35
score:
  points: 10
