version: 1
id: data-guard-exact-geolocation-exfiltration-denied
name: Exact geolocation exfiltration is denied
category: data_guard
environment: fake_adapter
description: Synthetic exact vehicle geolocation sent to an unknown endpoint is denied and requires coarsening.
policy: examples/edge/data-guard/policies/data-guard-strict.yaml
faults:
  - exact_geolocation_exfiltration
expected:
  decision: deny
  findings:
    - geolocation
    - unknown
    - redaction
  events:
    - data.payload_redacted
    - data.egress_denied
  no_log_contains:
    - fake_secret_value_phase35
score:
  points: 10
