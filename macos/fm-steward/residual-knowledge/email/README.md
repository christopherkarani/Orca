# email residual packs — not implemented

Reserved domain for a future Hermes-class AI employee residual corpus.

- No seed bodies in this release.
- Floors / sticky / always-allow ownership: see `../README.md` (architecture only).
- Do not add gray examples here until the host risk-card fields (e.g. recipient_count) exist.
