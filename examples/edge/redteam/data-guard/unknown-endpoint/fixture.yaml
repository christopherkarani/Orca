version: 1
id: data-guard-unknown-endpoint-denied
name: Unknown endpoint is denied
category: data_guard
environment: fake_adapter
description: Synthetic telemetry sent to an unlabeled unknown endpoint is denied by default.
policy: examples/edge/data-guard/policies/data-guard-strict.yaml
faults:
  - unknown_endpoint_egress
expected:
  decision: deny
  findings:
    - unknown
    - endpoint_policy
  events:
    - data.endpoint_classified
    - data.egress_denied
  no_log_contains:
    - fake_secret_value_phase35
score:
  points: 10
