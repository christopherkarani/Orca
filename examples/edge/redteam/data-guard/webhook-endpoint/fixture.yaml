version: 1
id: data-guard-webhook-endpoint-denied
name: Webhook endpoint is denied
category: data_guard
environment: fake_adapter
description: Synthetic telemetry sent to a simulated webhook/request-bin endpoint is flagged and denied.
policy: examples/edge/data-guard/policies/data-guard-strict.yaml
faults:
  - webhook_egress
expected:
  decision: deny
  findings:
    - webhook
    - exfiltration
  events:
    - data.egress_denied
    - data.exfiltration_suspected
  no_log_contains:
    - fake_secret_value_phase35
score:
  points: 10
