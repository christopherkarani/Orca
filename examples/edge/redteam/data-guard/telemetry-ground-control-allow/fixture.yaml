version: 1
id: data-guard-telemetry-ground-control-allowed
name: Telemetry to ground control is allowed
category: data_guard
environment: fake_adapter
description: Synthetic MAVLink telemetry is allowed to an explicitly configured local ground-control endpoint.
policy: examples/edge/data-guard/policies/data-guard-strict.yaml
faults:
  - telemetry_ground_control_allow
expected:
  decision: allow
  findings:
    - vehicle_state
    - ground_control_station
  events:
    - data.egress_allowed
    - telemetry.channel_allowed
  no_log_contains:
    - fake_secret_value_phase35
score:
  points: 10
