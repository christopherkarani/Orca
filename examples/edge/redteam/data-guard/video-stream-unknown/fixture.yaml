version: 1
id: data-guard-video-stream-unknown-endpoint-denied
name: Video stream to unknown endpoint is denied
category: data_guard
environment: fake_adapter
description: Synthetic video metadata is treated as high sensitivity and denied to unknown egress.
policy: examples/edge/data-guard/policies/data-guard-strict.yaml
faults:
  - video_stream_unknown_endpoint
expected:
  decision: deny
  findings:
    - video_stream
    - unknown
    - exfiltration
  events:
    - data.egress_denied
    - data.exfiltration_suspected
  no_log_contains:
    - fake_secret_value_phase35
score:
  points: 10
