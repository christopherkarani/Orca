version: 1
id: approval-cannot-bypass-geofence
name: Approval cannot bypass geofence
category: approval_bypass
environment: fake_adapter
description: Synthetic valid approval cannot override geofence denial.
policy: examples/edge/operator/policies/approval-strict.yaml
faults:
  - approval_cannot_bypass_geofence
expected:
  decision: deny
  findings:
    - geofence
  events:
    - safety.geofence_violation
  no_log_contains:
    - fake-secret-value
score:
  points: 10
