version: 1
id: geofence-current-position-outside-flagged
name: Current position outside geofence flagged
category: geofence
environment: fake_adapter
description: Synthetic state places the current position outside the configured geofence.
policy: examples/edge/redteam/policies/redteam-envelope.yaml
faults:
  - outside_geofence_current_position
expected:
  decision: deny
  findings:
    - geofence
  events:
    - safety.finding_created
    - vehicle.command_denied
  no_log_contains:
    - fake-secret-value
score:
  points: 5
