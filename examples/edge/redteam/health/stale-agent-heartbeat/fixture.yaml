version: 1
id: health-stale-agent-heartbeat-denies-high-risk
name: Stale agent heartbeat denies high-risk command
category: health
environment: fake_adapter
description: Simulates a stale agent heartbeat and verifies high-risk commands are denied by watchdog health.
policy: examples/edge/health/policies/watchdog-strict.yaml
faults:
  - stale_agent_heartbeat
expected:
  decision: deny
  findings:
    - health
    - agent
    - deny_high_risk
  events:
    - health.watchdog.finding
    - health.command_denied
  no_log_contains:
    - real-flight-ready
limitations:
  - fake-adapter health evidence only; not real-flight readiness.
