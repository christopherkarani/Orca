version: 1
id: altitude-mismatched-reference-denied
name: Mismatched altitude reference rejected
category: altitude
environment: fake_adapter
description: Synthetic set_altitude request uses an unsupported altitude reference.
policy: examples/edge/redteam/policies/redteam-envelope.yaml
faults:
  - mismatched_altitude_reference
expected:
  decision: deny
  findings:
    - altitude
  events:
    - safety.altitude_violation
    - vehicle.command_denied
  no_log_contains:
    - fake-secret-value
score:
  points: 10
