version: 1
id: mission-duplicate-item-handled
name: Duplicate mission item handled
category: mission
environment: fake_adapter
description: Synthetic mission contains a duplicate item sequence and is denied.
policy: examples/edge/redteam/policies/redteam-envelope.yaml
faults:
  - duplicate_mission_item
expected:
  decision: deny
  findings:
    - mission
  events:
    - safety.mission_item_denied
    - vehicle.command_denied
  no_log_contains:
    - fake-secret-value
score:
  points: 10
