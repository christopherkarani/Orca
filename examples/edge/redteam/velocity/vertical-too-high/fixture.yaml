version: 1
id: velocity-vertical-too-high-denied
name: Vertical velocity too high denied
category: velocity
environment: fake_adapter
description: Synthetic velocity request exceeds vertical velocity policy.
policy: examples/edge/redteam/policies/redteam-envelope.yaml
faults:
  - vertical_velocity_too_high
expected:
  decision: deny
  findings:
    - velocity
  events:
    - safety.velocity_violation
    - vehicle.command_denied
  no_log_contains:
    - fake-secret-value
score:
  points: 10
