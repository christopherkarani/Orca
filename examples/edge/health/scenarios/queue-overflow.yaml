id: queue-overflow
environment: fake_adapter
provenance: fake_adapter
command: takeoff
expected_decision: deny
health_fault: queue_overflow
expected_behavior: fail_closed
note: bounded command queue overflow fails closed in this strict scenario.
