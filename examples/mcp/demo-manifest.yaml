version: 1
server:
  name: demo
  transport: stdio
  command: python3
  args:
    - fixtures/mcp/fake_server.py
  expected_hash: null
  env:
    allow:
      - PATH
      - HOME
tools:
  list_files:
    risk: low
    default: allow
  get_status:
    risk: low
    default: allow
  run_command:
    risk: critical
    default: deny
resources:
  default: ask
prompts:
  default: ask
sampling:
  default: deny
