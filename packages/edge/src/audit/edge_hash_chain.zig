const core = @import("orca_core");

pub const HashHex = core.audit.hash_chain.HashHex;
pub const eventHash = core.audit.hash_chain.eventHash;
pub const canonicalEventAlloc = core.audit.hash_chain.canonicalEventAlloc;
pub const writeEventJsonLine = core.audit.hash_chain.writeEventJsonLine;
